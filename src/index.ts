import { buildCardList, getCardText } from "./utils/build-tool";
import { Color } from "./enums";
import { DOMAIN, FourColor, Guild, Shard } from "./constants";

const constants = {
  Color,
  DOMAIN,
  FourColor,
  Guild,
  Shard,
};

export {
  buildCardList,
  getCardText,
  constants,
}
